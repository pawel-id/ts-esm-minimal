{
  "name": "ts-esm-minimal",
  "version": "1.0.0",
  "description": "Minimal Typescript to build native ESM example",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "tap",
    "build": "tsc",
    "dev:build": "tsc --watch",
    "dev:run": "node --enable-source-maps --watch ./dist/index.js",
    "start": "node ./dist"
  },
  "dependencies": {
    "debug": "^4.4.0"
  },
  "devDependencies": {
    "@types/debug": "^4.1.12",
    "tap": "^21.1.0",
    "typescript": "^5.8.2"
  }
}
